# Final Datasets Directory (`dataset/`)

This directory contains the final generated dataset splits formatted for T5 / LLM model training and evaluation.

## Subdirectories:

- `evaluation/`: Benchmark evaluation datasets (FLORES-200, human evaluation test sets, multi-level noise spectrum sets).
- `train/`: Generated training splits ready for sequence-to-sequence model training.
